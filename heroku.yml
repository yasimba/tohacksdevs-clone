setup:
    addons:
      - plan: heroku-postgresql
        as: DATABASE
  build:
    docker:
      client: client/Dockerfile
      server: server/Dockerfile
  release:
    command:
      - ./deployment-tasks.sh
    image: client
    image: server